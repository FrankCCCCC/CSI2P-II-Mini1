#include "../function.h"

Result compute(int x, int y, int z){
    x = (-x * 1) + 0 + -1991 + (-y * 1) + (-z / 1);
    y = (0 * -x) + -(1 * -x) + 0 - -12734 + (1 * -y) * (z % 1);
    z = (-x * 1) + (-y % 1) + 0 - - -12734 + (0 * -y) * (z % 1) * (x + 0);

    Result res = {x, y, z};
    return res;
}