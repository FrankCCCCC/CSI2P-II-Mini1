#include "../function.h"

Result compute(int x, int y, int z){
    ((((x))))=(y)=(((((z)))))++;
    --(x);
    y=-5 - --x + ++z;
    y = x = x % (-5) - y*-10 + (((z)))+ -9;
    z = (x++) - (x--) - -0 + -y;

    Result res = {x, y, z};
    return res;
}